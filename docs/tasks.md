# Tasks

## Backend

### TODO

- [ ] full rest доделать для всех видов ресурсов
- [ ] CSV `rowsTo[T]` uses reflection** — `repo/repo.go:388-446`: fragile, slow, type-unsafe CSV deserialization via `reflect`. Already in tasks.md but still high priority.
- [ ] sync
  - [ ] чтобы страница обновлялась у всех при изменении источника
  - [ ] ws взаимодействие
  - [ ] nats
- [ ] админка
- [ ] разделение источников помимо VK
- [ ] кеширование
- [ ] auth more +

### DONE

- [x] побольше логирования с разделением по уровням
- [x] rooms: docs, review, tests
- [x] много комнат
- [x] simple deploy
- [x] разгрузить main
- [x] auth
- [x] config
- [x] logging
- [x] CSV storage layer с тестами
- [x] Room entity + API
- [x] Sources API
- [x] WebSocket chat (max 2)
- [x] Content selection page
- [x] handler/service/repo разделение слоёв
- [x] thread safety
- [x] Docker Compose
- [x] auth — register/login/logout/me endpoints
- [x] auth middleware
- [x] login/register pages

## Client-side

### TODO

- [ ] **Унифицировать тёмную тему** — auth.css использует светлую тему, остальные страницы тёмную. Привести login/register к единому тёмному стилю, удалить `auth.css`. Объединить повторяющиеся стили (модалки, кнопки) в один `static/styles.css`.

- [ ] **Единая навигация** — добавить минимальную навигационную панель (тонкую, не навязчивую) на всех страницах: логотип (ссылка на главную), имя пользователя, кнопка выхода. Убрать дублирующиеся кнопки навигации из контента страниц.

- [ ] **Room page: только видео + чат** — убрать верхнюю панель с ссылками «← На главную», «К контенту →». Перенести кнопку выбора источника (для owner) в саму панель чата. Invite code показывать мелко в углу плеера или в чате. Никаких крупных шапок.

- [ ] **Адаптивность: планшеты (768-1024px)** — на iPad и подобных устройствах чат-панель не должна быть слишком узкой. Переключение на вертикальный стэк как на мобилках, но с более сбалансированным соотношением (50/50 вместо 40/60).

- [ ] **Адаптивность: мобильные телефоны** — гарантировать, что поле ввода чата всегда видно (не перекрывается клавиатурой, не обрезается). Использовать `dvh`/`svh` единицы, `position: sticky` для формы чата.

- [ ] **Кнопка копирования invite-ссылки** — на странице комнаты добавить кнопку «Копировать ссылку» для приглашения других участников.

- [ ] **Удаление источника из контента** — на странице `/content.html` добавить кнопку удаления для каждого источника. Бекенд TODO: `DELETE /api/sources/{id}`.

- [ ] **Редактирование источника** — добавить возможность изменить name/url существующего источника. Бекенд TODO: `PATCH /api/sources/{id}`.

- [ ] **Удаление комнаты** — на странице комнаты для owner отображать кнопку удаления комнаты (`DELETE /api/rooms/{invite_code}`). Подтверждение перед удалением.

- [ ] **Обработка ошибок и состояний загрузки** — добавить визуальные состояния загрузки (skeleton/spinner), обработку сетевых ошибок с возможностью повторной попытки.

- [ ] **Визуальный полишинг** — единая цветовая схема, небольшие hover-эффекты, скроллбар в тёмном стиле, улучшенная типографика. Минималистично, без излишеств.

- [ ] **Рефакторинг старых файлов** — после завершения основных изменений удалить неиспользуемые файлы: `auth.css`, `app.js`, `styles.css` (старый), проверить что ничего не сломалось.

### DONE

- [/] `web/demo/` сохранён (player-api-demo.html) — не трогать.
